-- To turn on a simple you should follow the next steps
{- First, import the function withChip in 'Fuyu.GPIO.Chip'
   (alternative we could use the umbrella module Fuyu.GPIO)
   
-} 
module Main where
import Fuyu.GPIO.Chip (withChip)
import qualified Fuyu.GPIO.Line as Line  


main :: IO () 
main = undefined
